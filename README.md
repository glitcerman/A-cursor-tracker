# A-cursor-tracker
This is just a cursor tracker make for fun. For you, with this application, you can see which region you uses the more.
You can use the python module "pyinstaller" for transform the "main.py" file to a "main.exe" file.
(I have use this command: "pyinstaller --onefile --windowed main.py")
